version: '3.3'
services:
  db:
    image: postgres:11.2
    ports:
      - "5432:5432"
    volumes:
      - ./db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER:postgres
      - POSTGRES_PASSWORD:password
      - POSTGRES_DB:db-data
volumes:
  db-data:
    driver: local